'use strict';
import angular from '../../../../node_modules/angular'

// Create the 'rules' controller
angular.module('rules').controller('RulesController', ['$scope', '$http', 'Authentication',
  function ($scope, $http, Authentication) {
  }
]);
